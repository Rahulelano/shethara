# Shethara Fashion Project Guidelines

- **Architecture**: Standard Vite + React Single Page Application (SPA) with `@tanstack/react-router`.
- **Styling**: Tailwind CSS v4 (`src/styles.css`).
- **Icons**: Lucide React (`lucide-react`).
- **State Management**: TanStack React Query (`@tanstack/react-query`).
